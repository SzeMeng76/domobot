# Core Telegram Bot Framework (with all optional dependencies)
python-telegram-bot[all]==22.8

# Kurigram (actively maintained Pyrogram fork, for MTProto API: DC ID detection, large file upload, inline media edit)
kurigram>=2.2.23
tgcrypto>=1.2.5  # Speeds up Kurigram

# Environment Configuration
python-dotenv>=1.0.1  # ParseHub requires >=1.0.1

# HTTP Requests
httpx==0.28.1
brotli==1.2.0  # Required for Brotli decompression (App Store uses br encoding)

# OpenAI API (for AI Anti-Spam)
openai==2.41.0

# Web Scraping
beautifulsoup4>=4.12.3  # ParseHub requires >=4.12.3, yfinance requires >=4.11.1
lxml>=5.3.0  # ParseHub requires >=5.3.0

# Google Play Scraper
google-play-scraper==1.2.7

# Task Scheduling
schedule==1.2.2

# Redis (with async support and performance optimizations)
redis[hiredis]==8.0.0

# MySQL Async Driver
aiomysql==0.3.2

# Internationalization and Number Formatting
Babel>=2.14.0

# JustWatch API for streaming providers
simple-justwatch-python-api==1.3.1

# Timezone Support
tzlocal==5.3.1
tzdata==2026.2
backports.zoneinfo==0.2.1; python_version<"3.9"

# WHOIS Query Libraries
ipwhois==1.3.0
whois21==1.4.7
python-whois==0.9.6
asyncwhois>=1.1.12
whodap>=0.1.14  # RDAP support for asyncwhois

# DNS Query Library
dnspython==2.8.0

# Data Validation and Settings Management
pydantic~=2.13.4

# RSS Feed Parser
feedparser==6.0.12

# Translation API
googletrans==4.0.2

# Yahoo Finance API
yfinance==1.5.1  # 最新版本：chunked fundamentals fallback、valuation改用timeseries API、price repair修复
pandas==3.0.3  # Latest stable version as of May 2026

# Social Media Parser (ParseHub) - 社交媒体解析功能
parsehub~=2.0.39  # 社交媒体聚合解析器，支持20+平台 (2.0.39: Instagram改用自研API/移除instaloader依赖/新增Snapchat/多线程分片下载)
cryptography>=49.0.0  # ParseHub依赖：用于小黑盒动态加密算法（RSA/AES/3DES）
yt-dlp>=2026.6.9   # 2026.6.9: curl_cffi 0.15.x 原生支持、exec安全限制、CVE修复
pytubefix>=10.10.1  # YouTube 下载库（10.10.1: improved nsig extraction for recent YouTube player variants）
ytmusicapi>=1.12.1  # YouTube Music API（搜索、排行榜、歌词）1.12.0: get_song_credits国际化改进
pillow>=12.3.0  # 图像处理库 (12.3.0: security fixes, WebP improvements)
pillow-heif>=1.4.0  # HEIF/HEIC 图片格式支持
lxml-html-clean>=0.4.5  # HTML 清理工具
markdown>=3.10.2  # Markdown 转换器
pyyaml>=6.0.3  # YAML配置文件解析
tenacity>=9.1.4  # 重试机制（用于图床上传）
telegraph>=2.2.0  # Telegraph API 库（用于发布长文本）

# NetEase Cloud Music (ID3/FLAC tag embedding, Rust-powered)
mutagen-rs>=0.2.7
pycryptodome>=3.23.0  # AES encryption for NetEase WeAPI

# AI Summary - Audio/Video Processing
pydub>=0.25.1  # 音频切片（Whisper转录大文件）
opencv-python-headless>=5.0.0.93  # 视频截图（转录失败时的fallback）(5.0: OpenCV 5.x with Python 3.13 support)
aiofiles>=25.1.0  # 异步文件读取（图片base64转换）

# Reddit API (使用 httpx，原生支持 SOCKS5 代理)
# curl-cffi for TLS fingerprinting (JSON endpoint bypass)
curl-cffi==0.15.0  # v0.15.0: HTTP/3 支持 + UDP SOCKS5 代理 + 新浏览器指纹 (chrome145/146, firefox147)

# Weather Visualization (天气图表可视化)
matplotlib>=3.10.9  # 图表绘制库
